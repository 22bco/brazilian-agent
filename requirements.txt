# Core
openai>=1.40.0            # SDK usado también para DeepSeek (OpenAI-compatible)
python-dotenv>=1.0.0      # carga de .env

# Webhook (paso 4)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0             # llamadas salientes a la API de Kapso

# Leads → Google Sheets (paso 3)
gspread>=6.0.0
google-auth>=2.30.0

# Tests / evals (paso 5)
pytest>=8.0.0
